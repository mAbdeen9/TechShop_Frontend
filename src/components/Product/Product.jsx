import styles from "./Product.module.css";

const Product = () => {
  return (
    <section className={`container ${styles.box}`}>
      <h3>Product Page</h3>
    </section>
  );
};

export default Product;
